# Web development archive

Historical development material. Not a current architecture source of truth.

当前架构事实只维护在 `docs/architecture/`、`docs/reference/` 与 `docs/operations/`。早期 Phase、implementation、report 和 fix 材料归档时必须保留原文，不得把其中的计划性描述重新当作当前实现。
