base {
    log_debug = off;
    log_info = on;
    daemon = off;
    redirector = iptables;
}

redsocks {
    // 本地监听端口，iptables REDIRECT 将流量导向这里
    local_ip = 127.0.0.1;
    local_port = 12345;

    // 上游 SOCKS5 代理
    ip = 127.0.0.1;
    port = 1080;
    type = socks5;
}
