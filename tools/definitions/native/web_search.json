{
  "name": "web_search",
  "description": "Search the web for real-time information. Use when the user asks about current events, facts, or any information beyond your knowledge cutoff.",
  "category": "native",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query string"
      },
      "max_results": {
        "type": "integer",
        "default": 5,
        "description": "Maximum number of results to return"
      }
    },
    "required": ["query"]
  },
  "metadata": {
    "version": "1.0.0",
    "author": "HpAgent",
    "tags": ["search", "web", "internet"],
    "requires_api_key": false
  }
}
